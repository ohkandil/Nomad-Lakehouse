import type { KeyLike, Keymap, KeymapEvent } from "../../index.js";
export type LeaderTrigger = KeyLike | Readonly<{
    key: KeyLike;
}> | readonly Readonly<{
    key: KeyLike;
}>[];
export interface LeaderOptions {
    trigger: LeaderTrigger;
    name?: string;
}
export declare function resolveLeaderTrigger(trigger: LeaderTrigger): KeyLike | undefined;
/**
 * Defines a token such as `leader` that the default parser can reference as `<leader>`.
 */
export declare function registerLeader<TTarget extends object, TEvent extends KeymapEvent>(keymap: Keymap<TTarget, TEvent>, options: LeaderOptions): () => void;
